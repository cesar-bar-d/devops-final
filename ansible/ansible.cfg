# Configuración de Ansible para el proyecto DevOps-Final
[defaults]
inventory           = ./hosts
host_key_checking   = False  # Lab local, no requiere verificación de clave RSA
retry_files_enabled = False  # No generar archivos .retry
deprecation_warnings = False